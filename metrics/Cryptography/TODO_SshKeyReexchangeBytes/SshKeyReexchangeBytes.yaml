# ====== Metadata ======
id: "SshKeyReexchangeBytes"
description: "Checks that the session keys are renewed after the recommended transmitted data. | TBD"
category: "Cryptography"
version: "1.0"
comments: ""
# ====== Configuration ======
configuration:
  p1:
    operator: "=="
    targetValue: "1"
