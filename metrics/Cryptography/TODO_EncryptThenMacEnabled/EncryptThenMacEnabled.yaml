# ====== Metadata ======
id: "EncryptThenMacEnabled"
description: "Checks that the extension `encrypt_then_mac` is not disabled. | TransportEncryption"
category: "Cryptography"
version: "1.0"
comments: ""
# ====== Configuration ======
configuration:
  p1:
    operator: "=="
    targetValue: "true"
