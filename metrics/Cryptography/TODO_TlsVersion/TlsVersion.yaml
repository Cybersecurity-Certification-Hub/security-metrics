# ====== Metadata ======
id: "TlsVersion"
description: "Checks that only secure TLS versions (e.g. TLS 1.2, TLS 1.3) are used. | SecurityFeature.Confidentiality.TransportEncryption (property: protocol and protocolVersion)"
category: "Cryptography"
version: "1.0"
comments: ""
# ====== Configuration ======
configuration:
  p1:
    operator: "isIn"
    targetValue: "TBD"
