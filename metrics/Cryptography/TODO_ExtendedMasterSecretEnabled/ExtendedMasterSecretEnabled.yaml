# ====== Metadata ======
id: "ExtendedMasterSecretEnabled"
description: "Checks that the extension "extended_master_secret" is not disabled. | TransportEncryption"
category: "Cryptography"
version: "1.0"
comments: ""
# ====== Configuration ======
configuration:
  p1:
    operator: "=="
    targetValue: "true"
