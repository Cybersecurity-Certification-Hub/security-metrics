# ====== Metadata ======
id: "InitialPasswordChangeEnabled"
description: "Checks, if password must be changed after first login.  | SecurityFeature.Authenticity.PasswordBasedAuthentication, Resource.Infrastructure.PasswordPolicy (properties for this should be defined)"
category: "IdentityManagement"
version: "1.0"
comments: ""
# ====== Configuration ======
configuration:
  p1:
    operator: "=="
    targetValue: "true"
