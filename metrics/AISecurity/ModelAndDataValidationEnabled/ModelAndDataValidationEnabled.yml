# ====== Metadata ======
id: ModelAndDataValidationEnabled
description: This rule assesses whether an [AIModel] and its [TrainingData] have [ModelAndDataValidation] [p1:enabled] correctly configured.
category: AISecurity
version: "1.0"
comments: Model and data validation ensures that both the model configuration and the associated data meet expected quality, schema, and security requirements before use.
# ====== Configuration ======
configuration:
  p1:
    operator: "=="
    targetValue: True

